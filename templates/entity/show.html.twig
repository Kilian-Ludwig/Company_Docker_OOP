{% extends "layout.html.twig" %}

{% block title %}{{ className }} Details{% endblock %}

{% block content %}
    <h1>{{ className }} Details</h1>

    <table class="detail-table">
        <tbody>
        {% for key, value in entity.getProperties(false,true) %}
            <tr>
                <th>{{ key }}</th>
                <td>{{ value }}</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
    <div class="actions">
        <a href="http://www.company.bbq/{{ entityName }}/update/{{ entity.id }}">
            <button class="btn btn-primary">Ändern</button>
        </a>
        <a href="http://www.company.bbq/{{ entityName }}/update/{{ entity.id }}"
           onclick="return confirm('Wirklich löschen?');">
            <button class="btn btn-danger">Löschen</button>
        </a>
        <a href="http://www.company.bbq/{{ entityName }}/show">
            <button class="btn btn-secondary">Zurück zur Liste</button>
        </a>
    </div>
{% endblock %}
