{% extends "layout.html.twig" %}

{% block title %}
    {% if action == 'create' %}
        create {{ className }}
    {% else %}
        update {{ className }}
    {% endif %}
{% endblock %}

{% block content %}
    <h1>
        {% if action == 'create' %}
            create {{ className }}
        {% else %}
            update {{ className }}
        {% endif %}
    </h1>

    <form method="post" class="form">
            {% if entity %}
                {% if action == 'create' %}
                    {% set create = true %}
                {% else %}
                    {% set create = false %}
                {% endif %}

                {% set props = entity.getProperties(false,true,create) %}
                {% for key, value in props %}
                    {% if key != 'id' and key != 'createdAt' and key != 'updatedAt' %}
                        <div class="form-row">
                            <label for="{{ key }}">{{ key }}</label>
                            <input
                                    type="{{ entity.getFieldType(key) }}"
                                    id="{{ key }}"
                                    name="{{ key }}"
                                    value="{{ value }}"
                                    {% if key =="isActive" or key =="isHiring"%}
                                        min="0"
                                        max="1"
                                        step="1"
                                    {% endif %}
                                    required
                            >
                        </div>
                    {% endif %}
                {% endfor %}
            {% endif %}

        <div class="form-actions">
            <button type="submit" class="btn btn-primary">Save</button>
            <a href="http://www.company.bbq/{{ entityName }}/show">
                <button type="button" class="btn btn-secondary">Cancel</button>
            </a>
        </div>
    </form>
{% endblock %}
